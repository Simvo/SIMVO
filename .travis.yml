language: php

php:
  - 5.6

services: 
  - mysql

test:
  adapter: mysql2
  database: simvo_test
  username: root
  encoding: utf8

before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS test;'

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  

script:
  - vendor/bin/phpunit